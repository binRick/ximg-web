services:
  web:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./public-html:/usr/local/apache2/htdocs

  internet:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./internet-html:/usr/local/apache2/htdocs

  apps:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./apps-html:/usr/local/apache2/htdocs

  linux:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./linux-html:/usr/local/apache2/htdocs

  butterfly:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./butterfly-html:/usr/local/apache2/htdocs

  ascii:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./ascii-html:/usr/local/apache2/htdocs

  json:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./json-html:/usr/local/apache2/htdocs

  mario:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./mario-html:/usr/local/apache2/htdocs

  poker:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./poker-html:/usr/local/apache2/htdocs

  monkey:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./monkey-html:/usr/local/apache2/htdocs

  doom:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./doom-html:/usr/local/apache2/htdocs

  yaml:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./yaml-html:/usr/local/apache2/htdocs

  docker:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./docker-html:/usr/local/apache2/htdocs

  pizza:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./pizza-html:/usr/local/apache2/htdocs

  kombat:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./kombat-html:/usr/local/apache2/htdocs

  chinese:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./chinese-html:/usr/local/apache2/htdocs

  wargames:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./wargames-html:/usr/local/apache2/htdocs

  moto:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./moto-html:/usr/local/apache2/htdocs

  india:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./india-html:/usr/local/apache2/htdocs

  guns:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./guns-html:/usr/local/apache2/htdocs

  america:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./america-html:/usr/local/apache2/htdocs

  wood:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./wood-html:/usr/local/apache2/htdocs

  florida:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./florida-html:/usr/local/apache2/htdocs

  tampa:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./tampa-html:/usr/local/apache2/htdocs

  computers:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./computers-html:/usr/local/apache2/htdocs

  trump:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./trump-html:/usr/local/apache2/htdocs

  cnc:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./cnc-html:/usr/local/apache2/htdocs

  ximg-app:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./ximg-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  vr:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./vr-html:/usr/local/apache2/htdocs

  nav:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./nav-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  tmux:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./tmux-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  ansible:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./ansible-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  chess:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./chess-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  programming:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./programming-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  git:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./git-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  crypto:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./crypto-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  simcity:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./simcity-html:/usr/local/apache2/htdocs

  kart:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./kart-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  grilling:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./grilling-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  warcraft:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./warcraft-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  rx:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./rx-html:/usr/local/apache2/htdocs

  unixos:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./unix-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  bsd:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./bsd-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  mail:
    build: ./mail-server
    restart: unless-stopped
    ports:
      - "25:25"
    volumes:
      - ./mail-data:/mail-data

  fidonet:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./fidonet-html:/usr/local/apache2/htdocs

  passwords:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./passwords-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  coldwar:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./coldwar-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  pirates:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./pirates-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  medieval:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./medieval-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  awstats:
    build: ./awstats
    restart: unless-stopped
    init: true
    volumes:
      - ./logs:/logs:ro
      - ./awstats-data:/data
      - ./awstats-output:/output
      - ./shared-html:/shared-html:ro

  stats:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./awstats-output:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  change:
    build: ./change-server
    restart: unless-stopped
    volumes:
      - .:/repo:ro
      - ./shared-html:/app/shared:ro

  ids:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./ids-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  logs:
    build: ./logs-server
    restart: unless-stopped
    volumes:
      - ./logs:/logs:ro
      - ./ssh-logs:/ssh-logs:ro
      - ./logs-data:/data
      - /var/log/suricata:/var/log/suricata:ro

  systemd:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./systemd-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  ssh:
    build: ./ssh-server
    restart: unless-stopped
    hostname: web01
    ports:
      - "22:22"
    volumes:
      - ./ssh-logs:/ssh-logs
    cap_add:
      - NET_ADMIN
    networks:
      - ssh-net

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/shared.conf:/etc/nginx/shared.conf:ro
      - ./shared-html:/shared-html:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./public-html:/var/www/certbot:ro
      - ./logs:/var/log/nginx
    depends_on:
      - web
      - linux
      - butterfly
      - ascii
      - json
      - poker
      - logs

  mac:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./mac-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  ai:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./ai-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  claude:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./claude-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  bash:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./bash-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  zsh:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./zsh-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  vt101:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./vt101-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  vim:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./vim-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  httpsvc:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./http-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  sshsvc:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./ssh-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  sql:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./sql-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  space:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./space-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  coffee:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./coffee-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  japan:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./japan-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  quake:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./quake-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  nintendo:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./nintendo-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  physics:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./physics-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  chemistry:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./chemistry-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  biology:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./biology-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  math:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./math-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  dns:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./dns-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro
    networks:
      default:
        aliases:
          - dns-app

  evolution:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./evolution-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  suricata:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./suricata-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  nagios-web:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./nagios-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  nagios:
    build: ./nagios-server
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/log/suricata:/var/log/suricata:ro

  readme:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./readme-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro
      - ./README.md:/usr/local/apache2/htdocs/README.md:ro

  claudemd:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./claudemd-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro
      - ./CLAUDE.md:/usr/local/apache2/htdocs/CLAUDE.md:ro

  algorithms:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./algorithms-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  os:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./os-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  security:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./security-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  database:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./database-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  network:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./network-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  request:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./request-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  world:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./world-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  sandbox:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./sandbox-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  gravity:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./gravity-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  waves:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./waves-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  chaos:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./chaos-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  epidemic:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./epidemic-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  playground:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./playground-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  tokens:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./tokens-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  temperature:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./temperature-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  embeddings:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./embeddings-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  agents:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./agents-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  visualize:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./visualize-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  statslab:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./statslab-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  regression:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./regression-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  probability:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./probability-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  systemdesign:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./systemdesign-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  loadbalancer:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./loadbalancer-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  cdn:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./cdn-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  queue:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./queue-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  brain:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./brain-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  sleep:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./sleep-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  nutrition:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./nutrition-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  training:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./training-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  punch:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./punch-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  terminal:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./terminal-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  mainframe:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./mainframe-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  circuit:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./circuit-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  logic:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./logic-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  protocol:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./protocol-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  arpanet:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./arpanet-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  regex:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./regex-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  jwt:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./jwt-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  cron:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./cron-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  color:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./color-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  dna:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./dna-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  cell:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./cell-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

  immune:
    image: httpd:2.4-alpine
    restart: unless-stopped
    volumes:
      - ./immune-html:/usr/local/apache2/htdocs
      - ./shared-html:/usr/local/apache2/htdocs/shared:ro

networks:
  ssh-net:
    driver: bridge

