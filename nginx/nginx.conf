events {}

http {
    error_log  /var/log/nginx/error.log;

    # Use Docker's internal DNS resolver; re-resolve upstreams every 10 s
    # so nginx picks up new container IPs after restarts without itself needing a restart.
    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    # Redirect HTTP → HTTPS, but pass ACME challenges through
    server {
        listen 80;
        server_name ximg.app www.ximg.app linux.ximg.app logs.ximg.app butterfly.ximg.app ascii.ximg.app json.ximg.app poker.ximg.app mario.ximg.app yaml.ximg.app doom.ximg.app monkey.ximg.app docker.ximg.app pizza.ximg.app kombat.ximg.app chinese.ximg.app wargames.ximg.app warcraft.ximg.app moto.ximg.app india.ximg.app wood.ximg.app guns.ximg.app tampa.ximg.app florida.ximg.app america.ximg.app computers.ximg.app trump.ximg.app cnc.ximg.app rx.ximg.app mail.ximg.app simcity.ximg.app internet.ximg.app change.ximg.app fidonet.ximg.app coldwar.ximg.app apps.ximg.app ansible.ximg.app chess.ximg.app passwords.ximg.app tmux.ximg.app programming.ximg.app git.ximg.app systemd.ximg.app vr.ximg.app nav.ximg.app ximg.ximg.app mac.ximg.app ai.ximg.app kart.ximg.app;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        include /etc/nginx/shared.conf;

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name ximg.app www.ximg.app;

        access_log /var/log/nginx/ximg.access.log;
        error_log  /var/log/nginx/ximg.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream web;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name butterfly.ximg.app;

        access_log /var/log/nginx/butterfly.access.log;
        error_log  /var/log/nginx/butterfly.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream butterfly;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name logs.ximg.app;

        access_log /var/log/nginx/logs.access.log;
        error_log  /var/log/nginx/logs.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000" always;

        # WebSocket upgrade
        location /ws {
            set $upstream_logs logs;
            proxy_pass          http://$upstream_logs:3000;
            proxy_http_version  1.1;
            proxy_set_header    Upgrade           $http_upgrade;
            proxy_set_header    Connection        "upgrade";
            proxy_set_header    Host              $host;
            proxy_set_header    X-Real-IP         $remote_addr;
            proxy_set_header    X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header    X-Forwarded-Proto $scheme;
            proxy_read_timeout  24h;
        }

        include /etc/nginx/shared.conf;

        location / {
            set $upstream_logs logs;
            proxy_pass         http://$upstream_logs:3000;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name json.ximg.app;

        access_log /var/log/nginx/json.access.log;
        error_log  /var/log/nginx/json.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream json;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name ascii.ximg.app;

        access_log /var/log/nginx/ascii.access.log;
        error_log  /var/log/nginx/ascii.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream ascii;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name poker.ximg.app;

        access_log /var/log/nginx/poker.access.log;
        error_log  /var/log/nginx/poker.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream poker;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name mario.ximg.app;

        access_log /var/log/nginx/mario.access.log;
        error_log  /var/log/nginx/mario.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream mario;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name monkey.ximg.app;

        access_log /var/log/nginx/monkey.access.log;
        error_log  /var/log/nginx/monkey.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream monkey;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name doom.ximg.app;

        access_log /var/log/nginx/doom.access.log;
        error_log  /var/log/nginx/doom.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream doom;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name yaml.ximg.app;

        access_log /var/log/nginx/yaml.access.log;
        error_log  /var/log/nginx/yaml.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream yaml;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name pizza.ximg.app;

        access_log /var/log/nginx/pizza.access.log;
        error_log  /var/log/nginx/pizza.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream pizza;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name docker.ximg.app;

        access_log /var/log/nginx/docker.access.log;
        error_log  /var/log/nginx/docker.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream docker;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name kombat.ximg.app;

        access_log /var/log/nginx/kombat.access.log;
        error_log  /var/log/nginx/kombat.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream kombat;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name chinese.ximg.app;

        access_log /var/log/nginx/chinese.access.log;
        error_log  /var/log/nginx/chinese.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream chinese;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name wargames.ximg.app;

        access_log /var/log/nginx/wargames.access.log;
        error_log  /var/log/nginx/wargames.error.log;

        ssl_certificate     /etc/letsencrypt/live/wargames.ximg.app/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/wargames.ximg.app/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream wargames;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name moto.ximg.app;

        access_log /var/log/nginx/moto.access.log;
        error_log  /var/log/nginx/moto.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream moto;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name india.ximg.app;

        access_log /var/log/nginx/india.access.log;
        error_log  /var/log/nginx/india.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream india;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name wood.ximg.app;

        access_log /var/log/nginx/wood.access.log;
        error_log  /var/log/nginx/wood.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream wood;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name guns.ximg.app;

        access_log /var/log/nginx/guns.access.log;
        error_log  /var/log/nginx/guns.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream guns;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name tampa.ximg.app;

        access_log /var/log/nginx/tampa.access.log;
        error_log  /var/log/nginx/tampa.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream tampa;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name florida.ximg.app;

        access_log /var/log/nginx/florida.access.log;
        error_log  /var/log/nginx/florida.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream florida;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name america.ximg.app;

        access_log /var/log/nginx/america.access.log;
        error_log  /var/log/nginx/america.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream america;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name computers.ximg.app;

        access_log /var/log/nginx/computers.access.log;
        error_log  /var/log/nginx/computers.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream computers;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name trump.ximg.app;

        access_log /var/log/nginx/trump.access.log;
        error_log  /var/log/nginx/trump.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream trump;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name cnc.ximg.app;

        access_log /var/log/nginx/cnc.access.log;
        error_log  /var/log/nginx/cnc.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream cnc;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name simcity.ximg.app;

        access_log /var/log/nginx/simcity.access.log;
        error_log  /var/log/nginx/simcity.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream simcity;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name internet.ximg.app;

        access_log /var/log/nginx/internet.access.log;
        error_log  /var/log/nginx/internet.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream internet;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name linux.ximg.app;

        access_log /var/log/nginx/linux.access.log;
        error_log  /var/log/nginx/linux.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream linux;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name rx.ximg.app;

        access_log /var/log/nginx/rx.access.log;
        error_log  /var/log/nginx/rx.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream rx;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name mail.ximg.app;

        access_log /var/log/nginx/mail.access.log;
        error_log  /var/log/nginx/mail.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream_mail mail;
            proxy_pass         http://$upstream_mail:3001;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name change.ximg.app;

        access_log /var/log/nginx/change.access.log;
        error_log  /var/log/nginx/change.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream_change change;
            proxy_pass         http://$upstream_change:3002;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name apps.ximg.app;

        access_log /var/log/nginx/apps.access.log;
        error_log  /var/log/nginx/apps.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream apps;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name coldwar.ximg.app;

        access_log /var/log/nginx/coldwar.access.log;
        error_log  /var/log/nginx/coldwar.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream coldwar;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name fidonet.ximg.app;

        access_log /var/log/nginx/fidonet.access.log;
        error_log  /var/log/nginx/fidonet.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream fidonet;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name ansible.ximg.app;

        access_log /var/log/nginx/ansible.access.log;
        error_log  /var/log/nginx/ansible.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream ansible;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name chess.ximg.app;

        access_log /var/log/nginx/chess.access.log;
        error_log  /var/log/nginx/chess.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream chess;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        http2 on;

        server_name passwords.ximg.app;

        access_log /var/log/nginx/passwords.access.log;
        error_log  /var/log/nginx/passwords.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream passwords;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name programming.ximg.app;

        access_log /var/log/nginx/programming.access.log;
        error_log  /var/log/nginx/programming.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream programming;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name tmux.ximg.app;

        access_log /var/log/nginx/tmux.access.log;
        error_log  /var/log/nginx/tmux.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream tmux;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name git.ximg.app;

        access_log /var/log/nginx/git.access.log;
        error_log  /var/log/nginx/git.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream git;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name systemd.ximg.app;

        access_log /var/log/nginx/systemd.access.log;
        error_log  /var/log/nginx/systemd.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream systemd;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name nav.ximg.app;

        access_log /var/log/nginx/nav.access.log;
        error_log  /var/log/nginx/nav.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream nav;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name ximg.ximg.app;

        access_log /var/log/nginx/ximg-app.access.log;
        error_log  /var/log/nginx/ximg-app.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream ximg-app;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name vr.ximg.app;

        access_log /var/log/nginx/vr.access.log;
        error_log  /var/log/nginx/vr.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream vr;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name mac.ximg.app;

        access_log /var/log/nginx/mac.access.log;
        error_log  /var/log/nginx/mac.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream mac;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name ai.ximg.app;

        access_log /var/log/nginx/ai.access.log;
        error_log  /var/log/nginx/ai.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream ai;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name warcraft.ximg.app;

        access_log /var/log/nginx/warcraft.access.log;
        error_log  /var/log/nginx/warcraft.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream warcraft;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name kart.ximg.app;

        access_log /var/log/nginx/kart.access.log;
        error_log  /var/log/nginx/kart.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream kart;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name grilling.ximg.app;

        access_log /var/log/nginx/grilling.access.log;
        error_log  /var/log/nginx/grilling.error.log;

        ssl_certificate     /etc/letsencrypt/live/ximg.app-0001/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ximg.app-0001/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        add_header Strict-Transport-Security "max-age=63072000" always;

        include /etc/nginx/shared.conf;

        location / {
            set $upstream grilling;
            proxy_pass         http://$upstream:80;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }
}
